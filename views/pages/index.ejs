<!DOCTYPE html>
<html lang="en">
<head>
          <%- include('../partials/head'); %>

</head>
<body class="container">
    <header>
        <!-- include header-->
          <%- include('../partials/header'); %>
    </header>
    <main>
<h1>My Travel Wishlist</h1>
<h2>a list of places i have visited so far and places i'd still like to see</h2>
<h3 class="mt-4">List of countries:</h3>
<ul>
    <% countries.forEach(function(country){ %>
<li>
    <strong><%=country.name%></strong>
    <br/>
    <small><%=country.alpha2code%></small>
</li>
    <% }); %>
</ul>
<h3 class="mt-4">Extend your Travel Wishlist:</h3>
<form class="w-50" action="/api/countries" method="POST">
        <div class="form-group">
          <label for="countryName">Name of Country</label>
          <input type="text" class="form-control" id="countryName" aria-describedby="countryNameHelp" placeholder="Country" name="name">
          <small id="countryNameHelp" class="form-text text-muted">In which country would you like to travel next?</small>
        </div>
        <div class="form-group">
          <label for="countryISO2">ISO Alpha 2</label>
          <input type="text" class="form-control" id="countryISO2" aria-describedby="countryISO2Help" placeholder="ISO Alpha 2 Code" name="alpha2Code">
          <small id="countryISO2Help" class="form-text text-muted">What the alpha 2 ISO code?</small>
        </div>
        <div class="form-group">
            <label for="countryISO2">ISO Alpha 3</label>
            <input type="text" class="form-control" id="countryISO3" aria-describedby="countryISO3Help" placeholder="ISO Alpha 3 Code" name="alpha3Code">
            <small id="countryISO3Help" class="form-text text-muted">Whats the alpha 3 ISO code</small>
          </div>
        <button type="submit" class="btn btn-primary">Add to list</button>
      </form>
</div>
</main>
<footer>
  <%- include('../partials/footer'); %>
</footer>
</body>
</html>